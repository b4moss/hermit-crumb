name: Setup Node.js and npm ci
description: Install Node from .nvmrc and restore npm cache, then npm ci

runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version-file: ".nvmrc"
        cache: npm

    - name: Install dependencies
      shell: bash
      run: npm ci
