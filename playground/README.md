# playground

本リポジトリ内のデモ／サンプルサイト置き場。Netlify preview（`preview-site` CD）の対象になる。

## 現状（Phase 0）

骨格のみ。`@b4moss/hermit-crumb` を workspace 依存として参照する。

## 今後

- **Phase 1**: `b4moss/git-template` の `doc-site` ブランチをここに一括移植し、`dev` / `generate` が通る状態にする
- **Phase 2 以降**: パッケージ module の利用側として保守する
