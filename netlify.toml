# Netlify Git integration builds/deploys the playground on merge to
# `preview-site`. PR checks live in `.github/workflows/doc-site.yml`.

[build]
  command = "npm ci && npm run typecheck:playground && npm run generate:playground"
  publish = "playground/.output/public"

[build.environment]
  NODE_VERSION = "22.19"
