# Netlify builds the monorepo playground (create-equivalent demo).
# Production branch must be `preview-site` (see docs/specs/delivery/cd.md).

[build]
  command = "npm ci && npm run generate:playground"
  publish = "playground/.output/public"

[build.environment]
  NODE_VERSION = "22.19"
